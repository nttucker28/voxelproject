using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Direction
{
    forward, //+z direction
    backwards, //-z direction
    right, // +x
    left, // -x direction
    up, // +y direction
    down // -ydirection
}
